# Session Handoff — 2026-06-24

Pick-up notes for resuming "Notebound" in a new chat. Canonical backlog
lives in auto-memory `backlog_priority.md`; this is a point-in-time snapshot.

## What shipped this session (both merged to `main`, branches deleted)

1. **Note ratings (backlog #8)** — merge commit `72b6d28`.
   - Migration `016_notes_rating.sql` (applied to Supabase project
     `dcejrbyujfcxartywpis` "Travel Diary App"): nullable `rating smallint`
     (1–5) + CHECK constraint `notes_rating_requires_rateable_category` limiting
     ratings to food/stay/activity/shopping.
   - `RATEABLE_CATEGORIES` + `isRateable()` in `src/services/noteHelpers.ts` —
     single source of truth.
   - `src/components/StarRating.tsx` — reusable; input mode (tap, tap-again-to-clear)
     and `readOnly` display mode, `size` 'small'|'medium'.
   - Star input in `NoteCaptureSheet` + `NoteEditSheet` (only when category is
     rateable; cleared on switch to non-rateable). Read-only stars on `NoteCard`.
   - `rating` threaded through `CreateNoteInput` → `PendingNote` → `drainQueue`
     insert, and `UpdateNoteInput` → `updateNote`.
   - Spec/plan: `docs/superpowers/{specs,plans}/2026-06-22-note-ratings*`.

2. **Venue-name precedence (backlog 3.5)** — merge commit `ea44378`.
   - `mergeTags` (`src/services/taggingHelpers.ts`) now applies
     **manual > AI-inferred venue > geocoder area**. Added optional
     `location_source` to `ExistingTags`; AI venue overrides any non-manual
     geocoder `place_name`, geocoder kept only as fallback when AI found none.
   - `tagNote` (`src/services/taggingService.ts`) passes `note.location_source`.
   - No migration, no `tag-note` edge-fn change. Deliberately NO POI-vs-street
     detector (iOS rarely returns clean POIs; Claude reads note text).
   - Spec/plan: `docs/superpowers/{specs,plans}/2026-06-24-venue-name-precedence*`.

## Repo state

- On `main`. Working tree clean. Full suite green: **301 tests / 32 suites**,
  `npx tsc --noEmit` clean.
- **15 commits on local `main` are NOT pushed** to `origin/main`. Push when ready
  (`git push`). User had not asked to push as of this handoff.

## Verification still pending (optional, low-risk)

- **Ratings:** on-device smoke test from plan Task 7 not yet run — rate a food
  note, confirm stars on the card, switch to General and confirm they vanish
  (the clear-on-switch / DB-constraint path). Logic is unit-tested.
- **Venue-name:** no UI to check; behavior is in the live tagging round-trip
  (capture near a venue → after `tag-note` runs, `place_name` should be the
  venue, not a street). Covered by unit tests.

## Next up: Public layer (#3 Explore + #4 privacy)

Both precursors (#8, 3.5) are now done — this was the point of doing them first.
This is a BIG item; recommend its own brainstorm and likely decomposition into
sub-specs (e.g. (a) `public_places` table + RLS + aggregation/trigger on
trip-end, then (b) Explore UI). Decided model is already captured in
`backlog_priority.md` "Public/community model (decided 2026-06-21)":
- Anonymized aggregate only (counts, never who/when). Dedicated `public_places`
  table, public-read RLS, dedupe on normalized `place_name` + city. Carries
  `visit_count` + `avg_rating` + `rating_count`.
- Category-gated (general never goes public). Global opt-out, default ON.
  Trip-end gated (place goes public only when trip completed). No moderation in V1.
- Explore tab = global destination grid by popularity; destination page =
  community map (category-filterable) + ranked places list.

To resume: read `backlog_priority.md` (auto-memory) for full context, then start
`superpowers:brainstorming` on the public layer.

## Workflow notes for the next session

- Branch from `main`, no worktrees (per user pref). Run app with `npm run ios`.
- No lint script. Test = `npx jest`. Types via Supabase MCP
  `generate_typescript_types` after any migration (`apply_migration`), project
  id `dcejrbyujfcxartywpis`.
- This session used: brainstorming → writing-plans → subagent-driven-development
  (fresh subagent per task) → finishing (merge --no-ff, delete branch).
